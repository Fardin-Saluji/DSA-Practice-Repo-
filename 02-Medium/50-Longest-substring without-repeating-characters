// Problem: Write a program to find the longest substring without repeating characters in a given string
// Input: string = "abcabcbb"
// Output: "abc"

function longestUniqueSubstring(str) {
  let start = 0;
  let char = 0;
  let end = 0;

  for(let char of str) {
    if (num > str) {
      char ="abc";
    } else if (num < str && num < first.str) {
      console.log(str)
    }
  }
}

const string = [abcabcbb];
console.log(longestUniqueSubstring(str));
